<h1> Tasks </h1>
<% @tasks.each do |task| %>
<h2>
  <%= check_box_tag 'done', '1', task.completed %>
  <%= link_to task.title, task_path(task.id) %></h2>
<% end %>

<p> <%= link_to "Add a new task", tasks_new_path%></p>

